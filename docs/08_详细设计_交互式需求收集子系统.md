# 详细设计：交互式需求收集子系统

## 1. 概述

交互式需求收集子系统负责手机模拟界面的渲染、组件面板和交互操作。包含5个模块：模拟器模块、组件面板模块、组件管理模块、布局管理模块、结果序列化模块。

> **设计变更记录（v0.1.2）**：
> - 移除原"拖拽控制模块"（DragDropController / DropZoneManager），组件排序和跨容器拖入由 vuedraggable 库承担
> - 从界面子系统移入"组件面板模块"（ComponentPanel），使拖拽数据流在同一子系统内闭环

## 2. 模块图

![交互式需求收集子系统_模块图](images/交互式需求收集子系统_模块图.png)

### 模块职责

| 模块 | 职责 |
|------|------|
| 模拟器模块 | 手机外壳CSS渲染，内部组件递归渲染（SimNode），容器内排序委托给 vuedraggable |
| 组件面板模块 | 展示组件和布局列表，渲染预览，通过 vuedraggable pull:clone 将组件拖入模拟器 |
| 组件管理模块 | 动态加载开发者提供的组件库 JS 文件，解析为 Vue 运行时组件 |
| 布局管理模块 | 解析默认布局节点树，提供预设设计方案 |
| 结果序列化模块 | 将模拟器编辑状态序列化为结构化JSON |

### 模块间关系

| 提供方 | 消费方 | 说明 |
|--------|--------|------|
| 组件管理模块 | 模拟器模块 | 提供运行时组件供 SimNode 动态渲染 |
| 组件管理模块 | 组件面板模块 | 提供运行时组件供 PanelPreview 预览渲染 |
| 组件面板模块 | 模拟器模块 | vuedraggable clone 拖入 LayoutNode 到 SimNode 容器 |
| 布局管理模块 | 模拟器模块 | 提供默认设计方案 |
| 结果序列化模块 | 模拟器模块 | 读取节点树并序列化输出 |

### 拖拽排序机制

组件的拖拽排序由 **vuedraggable** 库提供排序和跨容器移动能力，本子系统负责规定拖拽过程中的数据转换规则和放置约束。

**本子系统内的两个模块各持有一个 vuedraggable 实例，共享 `group="sim"`**：

- **组件面板模块**：`pull: 'clone', put: false`（只能拖出，不能拖入）
- **模拟器模块（SimNode 容器）**：默认配置（可接收、可排序）

拖拽数据全程在 Vue 响应式系统内传递，不经过 dataTransfer 字符串序列化。

这种设计的核心原则：**SimNode 是纯渲染组件，不持有任何拖拽事件逻辑；拖拽行为与组件渲染完全解耦。**

## 3. 构件图（类图）

![交互式需求收集子系统_类图](images/交互式需求收集子系统_类图.png)

## 4. 类详细说明

### 4.1 模拟器模块

#### PhoneFrame

手机外壳组件，纯CSS实现手机外观。渲染模拟器根节点。

| 属性/方法 | 类型 | 说明 |
|-----------|------|------|
| width | number | 模拟器宽度，默认375px |
| height | number | 模拟器高度，默认667px |
| statusBarText | string | 顶部状态栏文字 |
| render() | VNode | 渲染手机外壳，内含 SimNode 渲染根节点 |

PhoneFrame 的职责仅限于：
1. 渲染手机外壳（状态栏、屏幕区域、Home Indicator）
2. 为根级节点渲染 SimNode 实例
3. 处理模拟器为空时的初始放置：当 `nodeTree` 为空且用户从组件面板拖入组件时，自动创建一个根 VStack 并将组件加入其中

PhoneFrame 的根层（`phone-screen`）不使用 vuedraggable，使用普通 div。所有放置目标都是容器 SimNode 内部的 vuedraggable 实例。

PhoneFrame 不监听任何 document 级别事件，不执行 DOM 查询，不持有拖拽状态。

#### SimNode

递归渲染组件，是模拟器的核心渲染单元。只负责渲染，不负责拖拽。

| 属性/方法 | 类型 | 说明 |
|-----------|------|------|
| node | LayoutNode | 当前要渲染的布局节点 |
| metaMap | Map\<string, ComponentInfo\> | 类型名到组件元数据的映射 |
| selectedNodeId | string | 当前选中的节点ID |
| runtimeComponent | Component (shallowRef) | 已解析的 Vue 运行时组件 |
| meta | ComponentInfo (computed) | 根据 node.type 从 metaMap 查找的元数据 |
| isContainer | boolean (computed) | 是否为容器节点（有 children 数组） |
| normalizedProps | object (computed) | 经过类型转换后的属性值 |
| render() | VNode | 递归渲染节点 |

**渲染逻辑**：

| 情况 | 渲染方式 |
|------|---------|
| 容器节点 | 外层 vuedraggable（group="sim"），内嵌递归 SimNode |
| 非容器节点（有 runtimeComponent） | `<component :is="runtimeComponent" v-bind="normalizedProps" />` |
| 非容器节点（无 runtimeComponent） | 灰盒回退占位，显示类型名和 props JSON |

**属性规范化逻辑（normalizedProps）**：

| 属性条件 | 转换规则 |
|---------|---------|
| 属性名为 `items` | 逗号分割字符串 → 数组 |
| PropDef.type === 'number' | `Number()` 转换 |
| PropDef.type === 'boolean' 或已知布尔键（bold/block/isLink等） | `value === true \|\| value === 'true'` |
| 其他 | 原值保留 |

### 4.2 组件面板模块

#### ComponentPanel

组件面板容器，纯 CSS 定位面板（`position: fixed`），不使用 el-drawer，无 overlay。显隐由界面子系统 ToolBar 通过共享状态仓库控制。

包含组件和布局两个标签页，每个标签页内使用一个 vuedraggable 实例：

- `group: { name: 'sim', pull: 'clone', put: false }` — 只能拖出，不能拖入
- `clone` 回调（`metaToNode`）：将 ComponentInfo 转换为 LayoutNode

| 属性/方法 | 类型 | 说明 |
|-----------|------|------|
| open | boolean | 是否打开（由共享状态仓库驱动） |
| activeTab | string | 当前标签页（component/layout） |
| toggle() | void | 切换开关状态 |
| setActiveTab(tab) | void | 切换标签页 |
| close() | void | 关闭面板 |
| metaToNode(meta) | LayoutNode | clone 回调：ComponentInfo → LayoutNode |
| render() | VNode | 渲染组件面板 |

**metaToNode 转换规则**：

| 输出字段 | 来源 | 说明 |
|---------|------|------|
| LayoutNode.id | `generateId()` | 全局唯一 ID（格式 `node_{计数}_{时间戳}`） |
| LayoutNode.type | `ComponentInfo.name` | 组件类型标识 |
| LayoutNode.props | `{ ...ComponentInfo.defaultProps }` | 浅拷贝默认属性 |
| LayoutNode.children | `ComponentInfo.isContainer ? [] : undefined` | 容器初始化空数组 |

#### PanelPreview

组件预览卡片，在面板中渲染组件的预览效果。不参与拖拽逻辑（拖拽由外层 vuedraggable 处理）。

| 属性/方法 | 类型 | 说明 |
|-----------|------|------|
| meta | ComponentInfo | 组件元数据 |
| runtimeComponent | Component | 已解析的运行时组件 |
| previewProps | object (computed) | 预览用属性值（defaultProps 转换后） |
| loadPreviewComponent() | Promise | 异步加载预览组件 |
| render() | VNode | 渲染预览 |

### 4.3 组件管理模块

#### ComponentLoader

组件库加载器，动态加载开发者提供的组件库 JS 文件并解析为 Vue 运行时组件。

| 属性/方法 | 类型 | 说明 |
|-----------|------|------|
| loadedLibs | Map | 已加载的组件库映射（path → lib 对象） |
| runtimeComponentCache | Map | 已解析的运行时组件缓存（cacheKey → Component） |
| preloadLibraries(metas) | Promise | 预加载所有 sourceFile 指定的库文件 |
| resolve(meta) | Promise\<Component\> | 根据元数据解析为 Vue 运行时组件 |
| loadScriptLibrary(sourceFile) | Promise\<object\> | 动态插入 `<script>` 标签加载组件库 JS |
| createContainerComponent(tag) | Component \| null | 创建内置容器组件（VStack/HStack/Grid2Col） |
| injectStyle(styleId, cssText) | void | 注入组件 CSS 到 document.head（去重） |

**组件解析优先级**：

1. 内置容器（VStack/HStack/Grid2Col）→ createContainerComponent 直接创建
2. 外部组件库 → loadScriptLibrary 加载后通过 `__rtRegister` 回调获取
3. 找不到 → 返回 null，SimNode 使用回退渲染

#### ComponentInfo

组件描述信息数据类，同时承载组件和布局的描述。

| 属性 | 类型 | 说明 |
|------|------|------|
| name | string | 组件唯一标识名 |
| label | string | 显示名称 |
| category | string | 分类（导航组件/基础组件/组合组件/布局/测试组件） |
| icon | string | 图标标识 |
| props | PropDef[] | 属性定义列表 |
| defaultProps | object | 默认属性值 |
| sourceFile | string | 库文件路径 |
| componentTag | string | 注册标签名 |
| isContainer | boolean | 是否为容器布局 |
| maxChildren | number | 最大子组件数量，-1 为不限制 |

#### PropDef

属性定义数据类。

| 属性 | 类型 | 说明 |
|------|------|------|
| name | string | 属性名 |
| type | string | 类型（string/number/boolean/enum） |
| options | string[] | 枚举值（type=enum 时） |
| default | any | 默认值 |
| label | string | 显示名称 |

### 4.4 布局管理模块

#### LayoutResolver

布局解析器，处理默认设计方案和布局节点操作。

| 属性/方法 | 类型 | 说明 |
|-----------|------|------|
| resolve(defaultLayoutId) | LayoutNode[] | 从 default_layout.json 中解析指定 ID 的布局方案 |
| createNode(type, props) | LayoutNode | 创建新的布局节点 |
| cloneTree(nodes) | LayoutNode[] | 深拷贝布局节点树 |

### 4.5 结果序列化模块

#### ResultSerializer

编辑结果序列化器。

| 属性/方法 | 类型 | 说明 |
|-----------|------|------|
| serialize(nodeTree) | object | 将节点树序列化为可存储的 JSON 结构 |
| deserialize(data) | LayoutNode[] | 将 JSON 数据反序列化为节点树 |

### 4.6 通用数据类

#### LayoutNode

布局节点，构成模拟器中的组件树。

| 属性 | 类型 | 说明 |
|------|------|------|
| id | string | 节点唯一 ID |
| type | string | 组件/布局类型名（对应 ComponentInfo.name） |
| props | object | 当前属性值 |
| children | LayoutNode[] | 子节点列表（容器布局才有） |

## 5. 模块对外接口

本子系统对外暴露 `I_Simulator`、`I_ComponentRegistry`、`I_LayoutRegistry` 三个接口（见子系统接口文档），各模块与接口的映射关系：

| 对外接口 | 实现模块 | 实现类/函数 |
|----------|----------|------------|
| I_Simulator | 模拟器模块 + 结果序列化模块 | SimNode + simulator store + ResultSerializer |
| I_ComponentRegistry | 组件管理模块 | ComponentLoader + app store |
| I_LayoutRegistry | 布局管理模块 | LayoutResolver + app store |

## 6. 实现文件映射

| 模块 | 实现文件 |
|------|----------|
| 模拟器模块 | src/modules/interactive/components/PhoneFrame.vue |
| | src/modules/interactive/components/SimNode.vue |
| 组件面板模块 | src/modules/interactive/components/ComponentPanel.vue |
| 组件管理模块 | src/modules/interactive/services/componentLoader.js |
| 布局管理模块 | src/stores/simulator.js（loadDefault） |
| 结果序列化模块 | src/stores/simulator.js（getResult） |
| 数据类 | src/stores/simulator.js（LayoutNode 结构） |

> **注意**：原设计中 ComponentRegistry、LayoutRegistry、LayoutResolver、ResultSerializer 规划为独立服务类文件，但实际实现中其职责已合并到 Pinia store（app.js / simulator.js）和 componentLoader.js 中。上表映射的是实际文件位置。
